import { usedThing } from "./deps";
// TODO: task-288 keep this linked
function smallTask(): void {
  usedThing();
}
const casted = payload as LegacyValue;
// non-null guarantee because validated above
const ensured = thing!.name;
const enum Status { Ready = "ready" }
try { work(); } catch (e: unknown) { report(e); }

---
id: route-142
title: "M56-001: network_whitelist.yml schema + identity.yml team_members field"
task_type: yaml-schema
milestone: M56
complexity: low
executor: copilot
context_required: [milestones/milestone-56-acp-integrity-command.md, audit-054]
files_affected: [agent/core/network_whitelist.yml, agent/core/identity.yml]
tokens_est: 2000
created: 2026-06-07
completed: 2026-06-08
---

# Route 142: Foundation Schema — network_whitelist.yml + identity.yml team_members

## Objective

Create the two prerequisite schema files needed by all downstream integrity scripts.

## Expected Output

### Files Created
- `agent/core/network_whitelist.yml` — bootstrap stub with schema per audit-054 INC-053-05

### Files Modified
- `agent/core/identity.yml` — add `team_members:` field (2 lines)

## Context

These are P0 prerequisites. `acp.git-provenance.sh` (route-145) needs `team_members` to verify commit authors. `acp.network-whitelist-validate.sh` (route-144) needs `network_whitelist.yml` to validate outbound calls.

## network_whitelist.yml Schema

```yaml
# agent/core/network_whitelist.yml
# Schema: list of approved outbound network targets
# Generated by acp-bootstrap.sh — customise for your project
version: "1.0"
approved_hosts:
  - "*.googleapis.com"
  - "*.firebase.google.com"
  - "*.firebaseio.com"
approved_ip_ranges: []
dynamic_hosts_allowed: false
last_reviewed: "2026-06-07"
reviewed_by: ""
```

## identity.yml Addition

```yaml
team_members: []
  # Add team member email addresses for IG-37 commit author verification
  # Example:
  #   - dev1@example.com
  #   - dev2@example.com
```

## Verification

- [ ] `network_whitelist.yml` parses as valid YAML
- [ ] `network_whitelist.yml` has all required schema fields
- [ ] `identity.yml` has `team_members:` field
- [ ] `acp-validate` passes with new files (no schema errors)

## User-Observable Acceptance

- `agent/core/network_whitelist.yml` exists with documented schema
- `agent/core/identity.yml` `team_members:` field is present (empty list by default)

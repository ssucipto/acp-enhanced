---
id: route-164
title: Fix AGENTS.md version header + add version-header check to /acp-validate
task_type: docs-update
milestone: M59
complexity: low
executor: copilot
context_required:
  - commands/acp.validate.md
files_affected:
  - AGENTS.md
  - agent/commands/acp.validate.md
tokens_est: 4000
tokens_actual:
cost_est_usd:
cost_actual_usd:
created: 2026-06-15
started:
completed:
override_reason:
---

## Objective

Update the stale `AGENTS.md` version header (v6.10.0 → current canonical version) and extend `/acp-validate` Step 2c version-consistency check to inspect the AGENTS.md prose version header so this drift is caught automatically in future.

## Context

`AGENTS.md:3` reads `> v6.10.0` while the project is at 6.12.1 (and will be 6.14.0 after M59). The three auto-loaded context files (AGENTS/CLAUDE/copilot-instructions) are body-synced via pre-commit hook but AGENTS.md carries an extra header line that drifts. `/acp-validate` v2.3.0 added a version-consistency check (lessons.md 2026-06-04) but it does not inspect this header. Found in audit-067 (MED-067-002).

## Steps

1. Update `AGENTS.md` header to the current version (coordinate with the M59 version bump to 6.14.0 — set to whatever version M59 ships).
2. Decide on the canonical version-stamp location and document it (recommend: the version lives in identity.yml/package.yaml; AGENTS.md header should reference it or be regenerated by the sync hook).
3. Extend `/acp-validate` Step 2c: read the AGENTS.md version header and compare to `identity.yml` version; warn (soft) on mismatch.
4. Optionally update the pre-commit sync hook to stamp the version header automatically (note as follow-up if out of scope).

## Expected Output

### Files Modified
- `AGENTS.md` — corrected version header
- `agent/commands/acp.validate.md` — Step 2c includes AGENTS.md header check

## Verification (double-verify)

- [ ] **Automated**: `/acp-validate` flags a deliberately mismatched AGENTS.md header
- [ ] **Manual**: AGENTS.md header version == identity.yml version
- [ ] Three-copy body sync still intact (AGENTS/CLAUDE/copilot bodies identical)

## User-Observable Acceptance

- AGENTS.md header shows the current version
- `/acp-validate` reports version consistency including AGENTS.md

## Addresses

audit-067 MED-067-002 (consolidated register M12)

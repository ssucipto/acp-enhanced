# Audit Report: Design Spec v2 — Second Round (v2 → v2.1)

**Audit**: #069  
**Date**: 2026-06-04  
**Subject**: `agent/reports/design-spec-app-interfaces-m15-spine-v2.md` — accuracy re-verification after audit-068  
**Baseline**: v2 spec (731 lines, 12 Mermaid diagrams); code under `frontend/`, `backend/server.py`  
**Deliverable**: `agent/reports/design-spec-app-interfaces-m15-spine-v2.1.md`

---

## Summary

Second-round audit re-checked v2 against the live codebase. **v2 remains structurally sound** — store inventory, tab re-export pattern, Firestore map, bootstrap sequence, roster dual path, `configuredDomains` truth table, PG traceability, and sign-out boundary (CO-285) all still match implementation.

**Four stale rows** in v2 were found (written before post-audit-068 code landed or carryover closure):

| ID | Severity | Issue | v2.1 fix |
|----|----------|-------|----------|
| G-069-01 | LOW | §10.2 task-174 “file missing” — `paytracker.test.tsx` exists with 12 tests | Mark ✅ fixed |
| G-069-02 | LOW | §10.2 task-175 “open” — `ClassicDashboard` removed; progress.yaml completed | Mark ✅ fixed |
| G-069-03 | LOW | §15 debt row for missing `paytracker.test.tsx` | Removed |
| G-069-04 | LOW | §16 M16 health cites “PG-06–PG-09” — health is **PG-08** only | Corrected |

**No new critical code defects** beyond audit-068’s **CO-285** (sign-out deletes `wageModel/current`). CO-272 carryover in `audit-carryovers.md` is still `status: open` but should be **closed** — file now exists.

**Verdict**: Adopt **v2.1** as the current interface spec. v2 is historical. CO-285 remains the primary blocker for PG-10 / task-177 sign-off criterion #11.

---

## Files Analyzed

| File | Relevance |
|------|-----------|
| `agent/reports/design-spec-app-interfaces-m15-spine-v2.md` | Subject |
| `frontend/__tests__/paytracker.test.tsx` | task-174 closure |
| `frontend/app/(tabs)/index.tsx` | Modern-only home; classic migration |
| `frontend/components/home/` | No `ClassicDashboard.tsx` |
| `frontend/store/authStore.ts` | signOut orchestration (CO-285) |
| `frontend/store/wageModelStore.ts` | `clearModel()` Firestore delete |
| `frontend/app/_layout.tsx` | Bootstrap listeners verified |
| `frontend/app/(tabs)/_layout.tsx` | Roster subscribe + mirror |
| `agent/progress.yaml` | task-174/175 completed |
| `agent/memory/audit-carryovers.md` | CO-272, CO-285 status |

---

## v2 Sections Re-Verified ✅

| Section | Finding |
|---------|---------|
| §4 Routes / tab re-exports | Matches `frontend/app/(tabs)/*.tsx` → `../wages`, etc. |
| §5 Store inventory (16 stores) | All files present under `frontend/store/` |
| §5.2 Roster read vs write | `rosterStore` write; `calendarStore.rosterPatterns` mirror read |
| §6 Firestore paths | Aligns with `userFirestoreCollections.ts` |
| §7 API catalog | Matches `backend/server.py` route groups |
| §12 Bootstrap sequence | `_layout.tsx:78-93` listeners + `spineMigration` |
| §12.1 Sign-out boundary | CO-285 still reproducible (`authStore.ts:241`, `wageModelStore.ts:251-260`) |
| §14 `configuredDomains` | `hasConfiguredRoster` + `payModel` hash conditions correct |
| §11 PG traceability | Status column still accurate (PG-08 open, PG-10 open) |
| Mermaid diagrams (12) | Syntax valid; no new stadium-node traps |

---

## Residual Open Items (unchanged from audit-068)

| Ref | Area | Notes |
|-----|------|-------|
| **CO-285** | Sign-out | `clearModel()` deletes FS wage doc — contradicts PG-10 |
| G-068-08 | Sign-out | `calendarStore.events` not cleared on sign-out |
| audit-037 | Shared device | tab/theme/pin AsyncStorage may persist across users |
| CO-261 | REST roster helpers | Dead code on `calendarStore` |
| G-063-04 | PDF OCR | JSON base64 upload path |
| CO-273 / task-177 | Device QA | Sign-off template unsigned |
| PG-08 | Health | M16 — not in v2.1 scope |

### Minor note (not spec-blocking)

`uiPreferencesStore.dashboardStyle` still accepts `'classic'` and `(tabs)/index.tsx` migrates legacy classic users to modern on mount. Component removed; preference enum remains for migration — acceptable; no v2.1 diagram change needed.

---

## Carryover Updates Recommended

| ID | Current | Recommended |
|----|---------|-------------|
| CO-272 | `open` | `fixed` — `paytracker.test.tsx` present, 12 tests |
| CO-285 | `open` | remain `open` until `clearModel` stops FS delete |

---

## Recommendations

1. **Use v2.1** for task-177 QA matrix and M16 interface planning.
2. **Fix CO-285** before device sign-off criterion #11 (“wage model restored after sign-out”).
3. **Run `/acp-design-spec`** for future milestone scopes (M16 health) instead of ad-hoc audits.
4. **Close CO-272** in `audit-carryovers.md` on next carryover hygiene pass.

---

**Audit type:** Documentation accuracy (round 2)  
**Generated by:** ACP `/acp-audit` #069
